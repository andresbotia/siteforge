# Scout

Manual lead-discovery agent. Orchestration lives in `src/lib/scout` and `src/data/scout.ts`.

Scout never receives `SUPABASE_SECRET_KEY` or `XAI_API_KEY`.
Basic qualification does not use paid AI.
Outreach, deploy, and payment side effects are forbidden.
